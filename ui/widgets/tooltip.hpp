#pragma once

namespace izan::ui {

// Tooltips in the kit's voice: body text, optional muted second line.
void kit_tooltip(const char* text);
void kit_tooltip_lines(const char* text, const char* hint);

}
